spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/hotel_management
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: ajutorparolamanagement
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  h2:
    console:
      enabled: false
  thymeleaf:
    cache: false

logging:
  level:
    root: INFO
    com.hotel: DEBUG
    org.springframework.security: INFO
  file:
    name: logs/hotel-dev.log
